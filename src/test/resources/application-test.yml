spring:
  datasource:
    url: jdbc:h2:mem:tickmeet;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000
tickmeet:
  demo: true
  seed: false
  scheduling: false
  storage-root: target/test-uploads
management:
  server:
    port: 0

